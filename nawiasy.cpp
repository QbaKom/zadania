#include <iostream>

using namespace std;

int main ()
{
    int n;
    cin >> n ;
    int x = 250;
    int t[x];           //odpowiada za wczytanie ciągu
    int t1[x];          //odpowiada za dowdawanie do stosu ()
    int t2[x];          //odpowiada za dodawanie do stosu []
    int t3[x];          //odpowiada za dodawanie do stosu {}
    
    for (int i = 1; i <= x; x++)
    {
        cin >> t[i]>>endl;

        if (t[i] = ( ; & t[i] = ); )
        {
            t1[x]++;
        }
        if (t[i] = [ ; & t[i] = ]; )
        {
            t2[x]++;
        }
        if (t[i] = { ; & t[i] = }; )
        {
            t3[x]++;
        }
    }


    

    
 
    return 0;
}